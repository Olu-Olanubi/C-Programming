#ifndef MAIN_H
#define MAIN_H

/**
 * solution: returns true if the first argument(string) passed in ends with the 2nd argument (also a string).
 * @arg1: first argument
 * @arg2: second argument
 * Return: bool
*/
bool solution(char* arg1, char* arg2);

#endif